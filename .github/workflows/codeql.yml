name: CodeQL

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 3 * * 1'   # щотижневий прогін

permissions:
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze (Java/Kotlin + JS/TS)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 👉 для Android/Gradle збірки
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          # ВАЖЛИВО: для Kotlin використовується Java-екстрактор
          languages: 'java,javascript'   # JS покриє і TS
          queries: security-extended,security-and-quality

      # Скомпілювати проєкт, щоб CodeQL зібрав БД (для Kotlin обов’язково!)
      - name: Build with Gradle (Android)
        run: |
          chmod +x ./gradlew
          ./gradlew clean assembleDebug --no-daemon

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
